# Defines the $connection_upgrade map variable needed for WebSocket proxying.
# This file is included at the http {} level by the serversideup/php base image.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
