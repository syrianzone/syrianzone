import HouseClient from './HouseClient';

export default function HouseIndex() {
  return <HouseClient />;
}

/*
PORT STATUS
  source:     resources/js/Pages/House/Index.tsx (32 lines)
  confidence: high
  todos:      0
  notes:      Native layout owns metadata while the client retains all legislative data modes.
*/
