import PopulationClient from './PopulationClient';

export default function PopulationIndex() {
  return <PopulationClient />;
}

/*
PORT STATUS
  source:     resources/js/Pages/Population/Index.tsx (22 lines)
  confidence: high
  todos:      0
  notes:      Expo Router supplies the native layout and metadata for the atlas screen.
*/
