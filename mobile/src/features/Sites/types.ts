export interface Website {
  description: string;
  id: string;
  name: string;
  type: string;
  url: string;
}

/*
PORT STATUS
  source:     resources/js/Pages/Sites/types.ts (9 lines)
  confidence: high
  todos:      0
  notes:      The native API module replaces direct CSV access.
*/
