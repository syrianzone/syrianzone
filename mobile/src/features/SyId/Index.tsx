import SyidClient from './SyidClient';

export default function SyidScreen() {
  return <SyidClient />;
}

/*
PORT STATUS
  source:     resources/js/Pages/SyId/Index.tsx (17 lines)
  confidence: high
  todos:      0
  notes:      Expo Router metadata and the native screen replace the Inertia layout wrapper.
*/
